import styled from 'styled-components';
import {gray1, gray3} from '../styles/palette';

export const Container = styled.div`
  height: 100px;
  background-color: ${gray1};
  border-bottom: 1px solid ${gray3};
`;
